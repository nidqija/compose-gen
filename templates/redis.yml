name: Redis Cache (Port 6379)
services:
  image: redis:alpine
  container_name: dev-redis
  ports:
    - "6379:6379"
  volumes:
    - redis_data:/data
  restart: unless-stopped
volumes:
  redis_data: {}